services:
  fishdex:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fishdex
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "127.0.0.1:3000:3000"
